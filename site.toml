# Site configuration
title = "sergiorivera.dev"
description = "A simple blog built with a minimal static site generator"
base_url = "https://sergiorivera.dev"
language = "en"

# Author information
[author]
name = "Sergio Rivera"

# Home route settings
[home]
recent_posts = 5

# Handle external sources of content
[sync]
type = "obsidian"
path = "/Users/sergiorivera/Obsidian/Personal Vault/97. Blog"  # ! SECURITY ISSUE: may expose local paths on /data.json

[server]
port = 8000
host = "localhost"
open_browser = true

# Site customization
[params]
# Main settings
posts_per_page = 10
show_reading_time = true
date_format = "%B %d, %Y"
